const fields=[['intro','Name and preferred pronouns','Example: My name is Jordan, and my pronouns are she/her.'],['background','A little about your background','Share your program of study, interests, communities, or goals.'],['connection','A story, writer, or tradition that stays with you','This may be a book, poem, song, film, family story, speech, or cultural tradition.'],['course','What you hope to explore in AFAM Literature','Consider identity, history, language, freedom, community, joy, or resistance.'],['closing','Closing thought','End with one detail or question you want classmates to remember.']];let saved={};try{saved=JSON.parse(localStorage.getItem('afam-studio-draft')||'{}')}catch{}const prompts=document.querySelector('#prompts');fields.forEach(([id,label,hint])=>{const el=document.createElement('label');el.innerHTML=`${label}<small>${hint}</small><textarea id="${id}"></textarea>`;prompts.append(el);el.querySelector('textarea').value=saved[id]||'';el.querySelector('textarea').oninput=e=>{saved[id]=e.target.value;localStorage.setItem('afam-studio-draft',JSON.stringify(saved))}});let script='';const render=()=>{script=fields.map(([id])=>document.querySelector('#'+id).value.trim()).filter(Boolean).join('\n\n');document.querySelector('#draft').textContent=script||'Your generated script will appear here.';document.querySelector('#side-script').textContent=script||'Generate your script first.'};document.querySelector('#generate').onclick=render;document.querySelectorAll('.copy').forEach(b=>b.onclick=()=>script&&navigator.clipboard.writeText(script));document.querySelector('#clear').onclick=()=>{fields.forEach(([id])=>document.querySelector('#'+id).value='');saved={};script='';localStorage.removeItem('afam-studio-draft');render()};document.querySelectorAll('.mode button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.mode button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')});let recorder,stream,chunks=[],seconds=0,timer;const video=document.querySelector('#video'),start=document.querySelector('#start'),stop=document.querySelector('#stop'),download=document.querySelector('#download'),status=document.querySelector('#status');start.onclick=async()=>{try{stream=await navigator.mediaDevices.getUserMedia({video:true,audio:true});video.srcObject=stream;video.muted=true;await video.play();chunks=[];recorder=new MediaRecorder(stream);recorder.ondataavailable=e=>e.data.size&&chunks.push(e.data);recorder.onstop=()=>{const url=URL.createObjectURL(new Blob(chunks,{type:recorder.mimeType||'video/webm'}));video.srcObject=null;video.src=url;video.muted=false;video.controls=true;download.href=url;download.classList.remove('hidden')};recorder.start();seconds=0;timer=setInterval(()=>{seconds++;document.querySelector('#clock').textContent=Math.floor(seconds/60)+':'+String(seconds%60).padStart(2,'0')},1000);start.disabled=true;stop.disabled=false;status.textContent='Recording now…'}catch{status.textContent='Camera access was not available. Try browser settings or record on your phone.'}};stop.onclick=()=>{recorder.stop();stream.getTracks().forEach(t=>t.stop());clearInterval(timer);start.disabled=false;stop.disabled=true;status.textContent='Recording complete. Review or download your video.'};
